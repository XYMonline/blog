---
import { getCategoryCounts } from '../../lib/blog';

const categories = await getCategoryCounts();
---

<nav class="widget categories-widget" aria-label="文章分类">
	<h3 class="widget-title">分类<span class="count">{categories.length}</span></h3>
	<ul class="widget-body">
		{
			categories.length ? (
				categories.map(([name, count]) => (
					<li>
						<a href={`/archive/?category=${encodeURIComponent(name)}`}>
							<span class="label">{name}</span>
							<span class="num">{count}</span>
						</a>
					</li>
				))
			) : (
				<li class="empty">暂无分类</li>
			)
		}
	</ul>
</nav>

<style>
	ul {
		list-style: none;
		margin: 0;
		padding: 0.35rem;
		display: grid;
		gap: 2px;
	}
	li {
		margin: 0;
	}
	a {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.5rem;
		padding: 0.4rem 0.6rem;
		border-radius: 8px;
		color: var(--text);
		text-decoration: none;
		transition:
			background-color 0.2s ease,
			color 0.2s ease;
	}
	a:hover {
		background: rgb(var(--gray-light), 55%);
		color: var(--accent);
	}
	.label {
		font-size: 0.92em;
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
		min-width: 0;
	}
	.num {
		flex: none;
		padding: 0.05rem 0.5rem;
		border-radius: 999px;
		background: rgb(var(--gray-light), 70%);
		color: var(--muted);
		font-size: 0.78em;
		font-weight: 600;
	}
	.empty {
		padding: 0.75rem;
		color: var(--muted);
		font-size: 0.85em;
	}
</style>